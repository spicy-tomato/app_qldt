name: user_repository
description: Dart package which manage the user domain.
version: 1.0.0

environment:
  sdk: ">=2.11.0 <3.0.0"

dependencies:
  equatable: ^1.2.6
  meta: ^1.2.4
  shared_preferences: ^2.0.0