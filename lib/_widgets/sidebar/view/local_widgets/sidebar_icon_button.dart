import 'package:flutter/material.dart';

class SidebarIcon extends Icon {
  const SidebarIcon(
    IconData iconData, {
    Key? key,
  }) : super(
          iconData,
          key: key,
          color: Colors.white,
          size: 35,
        );
}
